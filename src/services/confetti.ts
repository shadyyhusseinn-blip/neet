/**
 * Confetti Service for Celebration Effects
 * Uses canvas-confetti library for success animations
 */

// import confetti from 'canvas-confetti';

export class ConfettiService {
  /**
   * Fire confetti celebration effect
   */
  fire(_options?: any): void {
    // Disabled for now
    return;
  }

  /**
   * Fire confetti from both sides
   */
  fireSides(): void {
    // Disabled for now
    return;
  }

  /**
   * Fire confetti from center
   */
  fireCenter(): void {
    // Disabled for now
    return;
  }

  /**
   * Fire confetti rain effect
   */
  fireRain(): void {
    // Disabled for now
    return;
  }
}

export const confettiService = new ConfettiService();
